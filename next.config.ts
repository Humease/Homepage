import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  // GitHub Pages 프로젝트 사이트(예: username.github.io/Homepage) 사용 시 아래 주석 해제 후 리포 이름 입력
  // basePath: "/Homepage",
  // assetPrefix: "/Homepage/",
};

export default nextConfig;
